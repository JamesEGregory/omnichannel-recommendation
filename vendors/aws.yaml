name: AWS
sweet_spot:
  scale: "Mid to very large (100+ agents), but can flex smaller"
  it_capacity: "Medium to High — benefits from in-house or partner AWS skills"
  industries: ["Public sector", "Enterprise", "Retail", "Financial services"]
main_strength: "Highly scalable cloud-native platform with deep AI/automation capabilities"

automation_depth:
  switchboard: true
  service_requests: true
  transactions: true
  complex_agentic: true

channels:
  voice: true
  chat: true
  email: true
  social: ["WhatsApp Business", "SMS", "Facebook Messenger"]
  notes: "Omnichannel via Amazon Connect + Lex bots; strong WhatsApp support"

integrations:
  telephony:
    pbx_teams: "Cloud-native, supports SIP trunking; can integrate with legacy PBX via SBC"
  crm:
    supported: ["Salesforce", "Zendesk", "Zoho", "ServiceNow"]
    notes: "Pre-built connectors; extensible via APIs"
  back_office:
    connectors: "Via API Gateway, Lambda, or RPA-adjacent integrations"
    typical: ["Revs & Bens", "Waste", "Social care", "Custom APIs"]

wem_analytics:
  native: "Contact Lens for conversation analytics, real-time dashboards"
  partner: "Optional external BI (e.g., QuickSight, Power BI)"
  gaps: "Some customers export data for deeper reporting"

commercials:
  model: "Consumption-based (per minute/transaction), not per agent"
  pricing_band: "Typically low-to-mid, but variable depending on usage"
  gotchas: "Costs can be unpredictable without careful volume modelling"

security:
  residency: ["UK", "EU"]
  certifications: ["ISO 27001", "Cyber Essentials", "GDPR-compliant"]
  notes: "Strong UK data residency options via AWS London region"

public_sector_proof:
  examples:
    - "Several UK local authorities using Amazon Connect for voice + digital"
    - "NHS 111 call automation pilots"
    - "Central gov departments for scalable helplines"
